apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dm-milestone2-chart.fullname" . }}-test-connection"
  labels:
    {{- include "dm-milestone2-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dm-milestone2-chart.fullname" . }}-frontend:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
